<Window x:Class="Demo_Shoes.VIews.AddProductView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Demo_Shoes.VIews"
        mc:Ignorable="d"
        WindowStartupLocation="CenterScreen"
        Title="Окно добавления товара" Height="450" Width="800">
    <Grid Margin="20">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="300"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <StackPanel Grid.Column="0" Margin="0,0,20,0">
            <Border BorderBrush="Black" BorderThickness="1" Height="200" Margin="0,0,0,10">
                <Image x:Name="ImgProduct" Stretch="Uniform"/>
            </Border>
            <Button Content="Выбрать изображение" Height="30" Click="BtnSelectImage_Click"/>
        </StackPanel>

        <ScrollViewer Grid.Column="1">
            <StackPanel>
                <Label Content="Наименование товара:"/>
                <TextBox x:Name="TbName"  Margin="0,0,0,10"/>

                <Label Content="Категория:"/>
                <ComboBox x:Name="CbCategory" DisplayMemberPath="Name" SelectedValuePath="Id" Margin="0,0,0,10"/>

                <Label Content="Производитель:"/>
                <ComboBox x:Name="CbManufacturer" DisplayMemberPath="Name" SelectedValuePath="Id" Margin="0,0,0,10"/>

                <Label Content="Поставщик:"/>
                <ComboBox x:Name="CbSupplier" DisplayMemberPath="Name" SelectedValuePath="Id" Margin="0,0,0,10"/>

                <Label Content="Единица измерения:"/>
                <ComboBox x:Name="CbUnit" DisplayMemberPath="Name" SelectedValuePath="Id" Margin="0,0,0,10"/>

                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <StackPanel Grid.Column="0" Margin="0,0,5,0">
                        <Label Content="Цена (руб):"/>
                        <TextBox x:Name="TbPrice" Margin="0,0,0,10"/>
                    </StackPanel>

                    <StackPanel Grid.Column="1" Margin="5,0,5,0">
                        <Label Content="На складе:"/>
                        <TextBox x:Name="TbCount" Margin="0,0,0,10"/>
                    </StackPanel>

                    <StackPanel Grid.Column="2" Margin="5,0,0,0">
                        <Label Content="Скидка (%):"/>
                        <TextBox x:Name="TbDiscount" Margin="0,0,0,10"/>
                    </StackPanel>
                </Grid>

                <Label Content="Описание:"/>
                <TextBox x:Name="TbDescription" TextWrapping="Wrap" Height="80" VerticalScrollBarVisibility="Auto" Margin="0,0,0,20"/>

                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                    <Button Content="Добавить" Width="120" Height="40" Margin="0,0,10,0" Click="BtnAdd_Click"/>
                    <Button Content="Отмена" Width="120" Height="40" Click="BtnCancel_Click"/>
                </StackPanel>

            </StackPanel>
        </ScrollViewer>
    </Grid>
</Window>
