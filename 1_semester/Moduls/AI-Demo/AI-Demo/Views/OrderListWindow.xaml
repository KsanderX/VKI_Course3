<Window x:Class="AI_Demo.Views.OrderListWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:AI_Demo.Views"
        mc:Ignorable="d"
        Title="Заказы" Height="450" Width="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Button x:Name="btnAddOrder" Content="Добавить заказ" Click="BtnAddOrder_Click" HorizontalAlignment="Left" Margin="10"/>

        <ListView x:Name="LvOrders" Grid.Row="1" MouseDoubleClick="LvOrders_MouseDoubleClick">
            <ListView.View>
                <GridView>
                    <GridViewColumn Header="ID" DisplayMemberBinding="{Binding Id}"/>
                    <GridViewColumn Header="Статус" DisplayMemberBinding="{Binding OrderStatus.StatusName}"/>
                    <GridViewColumn Header="Дата заказа" DisplayMemberBinding="{Binding OrderDate, StringFormat=d}"/>
                    <GridViewColumn Header="Дата доставки" DisplayMemberBinding="{Binding DeliveryDay, StringFormat=d}"/>
                    <GridViewColumn Header="Клиент (ФИО)" DisplayMemberBinding="{Binding User.Fio}"/>
                </GridView>
            </ListView.View>
            <ListView.ContextMenu>
                <ContextMenu>
                    <MenuItem Header="Удалить" Click="MenuItemDeleteOrder_Click"/>
                </ContextMenu>
            </ListView.ContextMenu>
        </ListView>
    </Grid>
</Window>
