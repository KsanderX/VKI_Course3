<Window x:Class="MyAppToday.View.AssignMastersView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:MyAppToday.View"
        mc:Ignorable="d"
        Title="AssignMastersView" Height="450" Width="600"
        WindowStartupLocation="CenterScreen">
    <Grid>
        <StackPanel Width="300" Height="350">
            <Label FontSize="24" Content="Назначение мастеров" HorizontalAlignment="Center"/>
            <Label FontSize="18" Content="Выберите заявку"/>
            <ComboBox Height="30" FontSize="18" ItemsSource="{Binding Requests}" DisplayMemberPath="Id" SelectedValuePath="Id" SelectedItem="{Binding SelectedRequest}"/>
            <Label FontSize="18" Content="Выберите мастера"/>
            <ComboBox Height="30" FontSize="18" ItemsSource="{Binding Masters}" DisplayMemberPath="Login" SelectedValuePath="Id" SelectedItem="{Binding SelectedMaster}"/>
            <Label FontSize="18" Content="Комментарий"/>
            <TextBox Height="30" FontSize="18" Text="{Binding Comment}"/>
            <Button Margin="0,10,0,0" Height="30" Content="Назначить мастера на заявку" Click="btnAssignMaster"/>
            <Button Margin="0,10,0,0" Height="30" Content="Назад" Click="btnOpenOperatorView"/>
        </StackPanel>
    </Grid>
</Window>
