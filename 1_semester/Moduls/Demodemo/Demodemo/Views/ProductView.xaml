<Window x:Class="Demodemo.Views.ProductView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Demodemo.Views"
        mc:Ignorable="d"
        WindowStartupLocation="CenterScreen"
        Title="Список товара" Height="550" Width="1100">
    <DockPanel>
        <DockPanel DockPanel.Dock="Top">
            <TextBlock DockPanel.Dock="Right" FontSize="16" >
                <Run Text="Добро пожаловать:" />
                <Run Name="runFIO"/>
            </TextBlock>
            <Button Content="Выйти" Width="80" Height="30" Click="btnExit_Click"/>
            <StackPanel/>
        </DockPanel>

        <StackPanel Orientation="Horizontal" DockPanel.Dock="Top" x:Name="spFind">
            <TextBlock Text="Поиск"/>
            <TextBox Width="100" Margin="10 0 0 0" Name="tbFind" TextChanged="tbFind_TextChanged"/>

            <RadioButton  Content="По возрастанию" Margin="10 0 0 0" Checked="RadioButton_Checked" />
            <RadioButton Checked="RadioButton_Checked" Content="По убыванию" Margin="10 0 0 0" />

            <TextBlock Text="Поставщик:" Margin="10 0 0 0"/>
            <ComboBox SelectedIndex="0" Name="CbSupplier" Width="120" Margin="10 0 0 0" DisplayMemberPath="Name"/>
        </StackPanel>
        <StackPanel/>

        <StackPanel Name="spCRUID" DockPanel.Dock="Bottom" Orientation="Horizontal" >
            <Button Content="Добавить товар" Width="100" Height="30" Margin="5" Name="btnAddProduct" Click="btnAddProduct_Click" />
            <Button Content="Удалить товар" Width="100" Height="30" Margin="5" Name="btnDeleteProduct" Click="btnDeleteProduct_Click"/>
        </StackPanel>


        <StackPanel Height="600" >
            <ListBox Height="450" ScrollViewer.VerticalScrollBarVisibility="Auto"
                         x:Name="listBoxProducts" HorizontalContentAlignment="Stretch" />
        </StackPanel>

    </DockPanel>
</Window>
